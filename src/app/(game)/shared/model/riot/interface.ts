export interface Summoner {
  accountId: string;
  profileIconId: string;
  revisionDate: number;
  id: string;
  puuid: string;
  summonerLevel: number;
}

export interface Account {
  gameName: string;
  tagLine: string;
  puuid: string;
}
